package com.kym.spring_boot_microservice_1_GestionUsuario.persistencia.validacion;


public class validacion {

   /* public ResponseDTO validarUsuario(Usuario usuario){
        ResponseDTO responseDTO = new ResponseDTO();

        if(usuario.getUsername()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("El nombre de usuario debe tener al menos 5 caracteres");
            return responseDTO;
        }

        if(usuario.getPassword()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("La contraseña debe tener al menos 5 caracteres");
            return responseDTO;
        }
        if (usuario.getNombre()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("El nombre debe tener al menos 5 caracteres");
            return responseDTO;
        }

        if(usuario.getApellido()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("El apellido debe tener al menos 5 caracteres");
            return responseDTO;
        }

        if(usuario.getDni()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("El DNI debe tener al menos 8 caracteres");
            return responseDTO;
        }

        if(usuario.getCorreo()==null){
            responseDTO.setNumeroError(responseDTO.getNumeroError()+1);
            responseDTO.setMensajeError("El correo debe tener al menos 5 caracteres");
            return responseDTO;
        }

        if(usuario.getCelular()==null) {
            responseDTO.setNumeroError(responseDTO.getNumeroError() + 1);
            responseDTO.setMensajeError("El celular debe tener al menos 9 caracteres");
            return responseDTO;
        }

        return responseDTO;

    }*/
}
